<%= form_for(@category_rep) do |f| %>
  <% if @category_rep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_rep.errors.count, "error") %> prohibited this category_rep from being saved:</h2>

      <ul>
      <% @category_rep.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
