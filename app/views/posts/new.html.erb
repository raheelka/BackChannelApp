<%= form_for @post do |f| %>
    <div class="field">
     <div> Category :<br/> <%= f.text_area :category %></div>
     <div> Content : <br/><%= f.text_area :content %> </div>
    </div>
    <div class="actions">
      <%= f.submit "Submit" %>
    </div>
<% end %>